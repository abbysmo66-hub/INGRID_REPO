#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <time.h>
#include <ctype.h>
#include <string.h>
#include <math.h>

/// Ejercicio 01 (Intermedio) - Clasificador de números
/// Un sistema de auditoría necesita analizar una secuencia de números ingresados por el usuario. El
/// sistema solicita la cantidad de números a procesar y luego, para cada uno, determina si es par o impar,
/// y si es positivo, negativo o cero.
/// Implementar las funciones necesarias para que main pueda mostrar, para cada número ingresado, su
/// clasificación completa. La cantidad de números a analizar se define con una constante. */

#define CANTIDAD 5
int esPar (int numero);
int esPositivo (int numero);
void mostrarClasificacion(int numero, int par, int positivo);
int main()
{
    int i, numero;
    printf ("---Clasificador de numeros---\n");
    printf ("\nProcesando %d numeros:\n\n", CANTIDAD);
    for (i = 0; i < CANTIDAD; i++)
    {
        printf ("Ingrese el numero %d: \t", i+1);
        scanf ("%d", &numero);
        /// Mostrar clasificacion
        mostrarClasificacion(numero, esPar(numero),positivo(numero));
        printf ("------------------------------\n");
    }
    return 0;
}
//Retorna 1 si es par y si es impar 0.
int esPar(int numero) {
    if(numero %2==0){
        return 1;
    }else {
    return 0;
    }
}

//Retorna 1 si es positivo, -1 si es negativo.
int positivo(int numero){
    if (numero>0){
        return 1;
    }else if (numero<0){
    return -1;
    }else{
    return 0;
    }
}

//Muestra el resultado final por pantalla.
void mostrarClasificacion(int numero, int par, int positivo)
    {
    printf("Resultado para el numero %d:\n", numero);

    //clasificación de paridad.
    if(par==1){
        printf("- Es Par\n");
    }else{
    printf("- Es Impar\n");
    }

    //Clasificacion de positivos.
    if (positivo==1) {
        printf("- Es Positivo\n");
    }else if (positivo==-1){
    printf ("- Es Negativo\n");
    }else {
    printf ("- Es Cero\n");
    }
}
